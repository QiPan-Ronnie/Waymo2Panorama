# DB-241 —— 四源数据集量产（把 koi 认可的配方移植到 AV2 之外）

立项 2026-08-16。上游 DB-239（B-route 与接缝裁决）、DB-240（08-14 会议契约与数据源核查）。
下游 = Louison 的 stage-2 微调。

**与 DB-240 的分工**：DB-240 回答"做什么"（会议契约、哪些数据源合格）；
DB-241 回答"怎么把同一个配方跑在四个 rig 上"。

---

## 1. Question

koi 在 AV2 上认可了一版规则接缝 mask（`clip_broute_rulemask.mp4`，08-14）。
**同一个配方能否原样跑在 nuScenes / Waymo Perception / Waymo E2E 上，
产出四个源各 ~500 个 93 帧样本？**

---

## 2. 已确立的地基（本轮已完成）

- ✅ **配方已恢复并固化**：原产出代码是未落盘的 heredoc，已从 transcript
  逐字恢复为 `agent/db240_rule_dataset/db240_koi_reference.py`
- ✅ **产线已对齐**：`db240_rule_dataset.py` 修正为 24 帧 wedge 并集 + 不闭合环检测，
  输出与 koi 认可的 mask **逐位相同**
- ✅ **golden test 已建并突变验证**：`test_db241_golden_rule_mask.py`，217 KB fixture，
  无需图像/LiDAR，五条断言
- ✅ **根基契约已成文**：`agent/db241_multids_production/FOUNDATION.md`
- ✅ **四个 rig 的真实几何已实测**（非查文档，从官方原始数据解析）

---

## 3. Kill criteria（先注册，后执行）

| 编号 | 若出现则该源出局或降级 |
|---|---|
| K1 | 去畸变后接缝带宽 > 150 px（AV2 为 58–68）⇒ 该 rig 的重叠区过大，规则条不再"薄" |
| K2 | 单样本黑区 > 60% of band ⇒ 真实像素太少，作为 GT 价值存疑，须回 koi |
| K3 | 环邻接推导出的配对数 ≠ 人工核对的物理相邻对数 ⇒ 该 rig 的 `adjacent_pairs` 不可用 |
| K4 | 93 帧窗口内相机数发生变化（掉帧/缺相机）⇒ 该场景丢弃，不做特判 |
| K5 | golden test 失败 ⇒ 停止一切量产，先修根基 |

---

## 4. 任务

### A. 抢救与固化（阻塞其余全部）
- **A1** 提交根基代码到特性分支（`db238/239/240/241` 四个目录）—— **待用户批准**
- **A2** 快照 `.worktrees/db213-root-artifact-fixes` 的 17 个未跟踪 adapter —— **待用户批准**

### B. 逐源移植（每源终点 = 1 个过 koi 眼验的样本）
- **B1 nuScenes**（最省事，先行）：原图已去畸变、时间戳/ego pose 齐全、6 相机闭环。
  下 `v1.0-mini`（4.17 GB，公开 S3 免登录）→ adapter → 产线 → 1 样本
- **B2 Waymo Perception**：需 **D2 去畸变**（k1=0.347，边缘 71 px）；
  环不闭合（130° 空洞）已由 `adjacent_pairs` 处理；
  预估单样本 ~48% 黑，**须连样本一起报给 koi**
- **B3 Waymo E2E**：需云端单遍 demux（全局洗牌，一段 200 帧散在 93 个分片）；
  去畸变（6.8 px）；`SKIRT_PX` 4→6 补 EMC 缺失
- **B4** 四个样本齐了一起过 koi 的眼验门（DB-240 B7）

### C. 量产
- **C1** 各源 ~500 样本；车头两版按场景 50/50
- **C2** OOD holdout 选定（PandaSet 80 帧的去向须先由 koi 定）
- **C3** 每源内部 train/test 7:3 或 8:2
- **C4** README：哪些包给 Louison、哪些留下、划分依据

### D. 交付前必答
- **D1** 向 Louison 确认 **stage-2 的 loss 是否已乘 mask**
  （koi 0814:249 用了"这一次"，说明是改动；stage-1 现有极性相反，无证据表明已改）

---

## 5. 实测几何账本（四个 rig）

| | AV2 | nuScenes | Waymo Percep | Waymo E2E |
|---|---|---|---|---|
| 相机数 | 7 | 6 | 5 | 8 |
| 环闭合 | ✓ | ✓ | **✗ 130° 洞** | ✓（45° 均匀） |
| rig 半径 | 0.209–0.378 m | — | **0.130 m**（视差更小） | 0.192 m |
| EMC | ✓ | ✓ | ✓ | **✗ 时间戳全 0** |
| 畸变 max\|k\| | 原生 pinhole | **已去畸变** | **0.347 → 71 px** | 0.0736 → 6.8 px |
| 预估黑区 | 21.5% | 待测 | **~48%** | 待测 |

**更正记录**：Perception 的空洞是 **130°** 不是 108°。
官方「252°」是把 5 个相机 FOV 直接相加（5×50.4），重复计入相邻 5° 重叠；
真实标定给出方位覆盖 230°（相机中心 ±90.2°，各半 FOV 25.0°）。

---

## 6. Max scope

本 DB 只做"把已认可的配方搬到其他 rig"。**不重开**接缝算法路线
（DB-239 已证伪 24 条），**不重做** mask 形态选择（koi 已批当时那一版；
若新 rig 上形态需变，走眼验门重新给他看，不自行决定）。
