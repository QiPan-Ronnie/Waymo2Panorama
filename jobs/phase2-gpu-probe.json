{
  "id": "phase2-gpu-probe",
  "cmd": [
    "bash",
    "-c",
    "python -c \"import torch; print('cuda_available:', torch.cuda.is_available()); print('device:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'cpu'); print('capability:', torch.cuda.get_device_capability(0) if torch.cuda.is_available() else None); print('mem_total_GB:', round(torch.cuda.get_device_properties(0).total_memory/1024**3,2) if torch.cuda.is_available() else None); print('torch:', torch.__version__)\" 2>&1 | tee /tmp/gpu-probe.log; touch /tmp/phase2-gpu-probe.done"
  ],
  "done_marker": "/tmp/phase2-gpu-probe.done",
  "timeout_s": 60,
  "created_at": "2026-05-20T05:03:18.297996+00:00"
}