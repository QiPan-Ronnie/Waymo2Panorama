# PLAN — PanoFuse (plausible 360 synthesis) — 2026-05-30

**Headline**: CertiPano: a geometrically certifiable 360Â° panorama for AV world-model training â€” a rigid far-field-exact backbone (L1) plus a provenance-gated, object-safe seam synthesizer whose one novel mechanism is fused-3DGS-as-referee disambiguation (the feed-forward 3DGS render arbitrates which of the two doubled copies is real, then a seam-confined diffusion refiner sharpens it), validated by a causal downstream world-model utility probe.

**Why it works now**: Under the OLD source-faithful bar, the in-band wide-baseline parallax is 2D-under-determined (the view center between cameras is seen by no neighbor; cameras overlap only ~18.6Â°), so every faithful fusion route was correctly NEG'd: depth-reproject (E2, all sources incl. 9-sweep LiDAR), flow-warp (E3, starves on textureless mid-range), plain inpaint (E4), confined DrivingForward render (E5, too warp/blur), held-out reconstruction (coverage-NEG). There is no real evidence to reconstruct the missing pixels faithfully, so the project over-rejected good-looking fills for 16 versions. The PLAUSIBLE bar removes the impossible requirement (pixel-for-pixel truth behind a few-cm seam) and replaces it with three achievable ones: photoreal + structure-continuation + no invented salient objects. That makes generation legitimate exactly where evidence is absent, while the rigid L1 backbone + hard re-paste keep the 99%+ of the panorama that DOES have evidence byte-exact and certifiable. Crucially, the relaxation is bounded, not blanket: the THINGS-vs-STUFF construction and the invention audit keep the relaxation from corrupting object statistics â€” the one thing a world model must not be taught wrong. So the bar change converts an under-determined reconstruction problem (impossible) into a constrained, auditable continuation problem (tractable), and turns the user's liked image from "over-rejected" into "the expected acceptable output."

**First thing to run**: Run the S0 ORACLE probe on the BMW near-white-car doubling anchor: render the AV2-finetuned DrivingForward single-center G_erp, then use it plus LiDAR depth to SELECT (not warp, not diffuse) which of the two L1 doubled copies is the true instance per masked column, alpha-composite the selected real pixels into E1.5 inside that one seam band, and eyeball a 4-up crop (E1.5 doubled / referee-selected / no-referee single-copy / L1). Decision rule: if the car collapses to ONE clean instance without smear or boundary kink and the far field is untouched, the fused-referee mechanism â€” the entire method-paper novelty â€” is real and worth building a diffusion refiner around; if even this oracle copy-select cannot single the car, the referee is dead and we immediately pivot to the dataset/framework + outpaint paper (which is independently shippable). This is ~1 A100-hour, builds nothing, and tests the one assumption everything else rests on.

## Contributions
- PROBLEM FORMULATION: wide-baseline (21â€“26cm), low-overlap (~18.6Â°), non-co-located surround-pinhole â†’ clean 360Â° ERP as world-model TRAINING DATA, under a PLAUSIBLE+structure-continuation+no-invented-objects bar; first clean treatment of this AV-specific niche.
- FUSED-3DGS-AS-REFEREE (the one defensible mechanism): a multi-view-fused single-center render disambiguates which of two near-field doubled copies is real â€” a source single-frame depth-reprojection provably lacks â€” used as a selector/prior, not as pixels, so it never imports the 3DGS global warp. (Carried by the soft-referee vs hard-LiDAR ablation.)
- GEOMETRY/APPEARANCE DECOUPLING with a MEASURED far-field bound: generation confined to a parallax-budget seam mask; far field byte-exact by hard re-paste; we report relative_warp as a sanity invariant AND quantify residual edge/line drift INSIDE the band (not just where it is trivially zero).
- OBJECT-SAFE SEAM SYNTHESIS for world-model data: THINGS composited (never generated), STUFF continued; an invention audit with a MEASURED guard recall (synthetic-object injection) + LiDAR cross-check â€” a safety property no cited panorama/3DGS method offers.
- TWO RULERS the field lacks: relative_warp (far-field geometric fidelity) + a new seam-band structural-continuity / object-invention-rate metric â€” reusable, citable evaluation protocol.
- CAUSAL DOWNSTREAM EVIDENCE: a pre-registered world-model utility probe (train-on-our-360 vs L1 vs perspective; rollout/prediction metric + spurious-object audit) â€” the one result reviewers cannot wave away.
- PROVENANCE-GATED DATA CONTRACT: per-pixel REAL/SYNTH channel + per-frame manifest so a downstream trainer can mask/down-weight synthesized pixels â€” a systems/dataset contribution.

## Staged plan
### [S0 â€” Oracle de-risk of the ONE load-bearing assumption (no diffusion, no build)] Decide on day one whether fused-3DGS-as-referee can make the worst near-field doubled object single, BEFORE building any pipeline.
- steps: On the 3 anchors (esp. BMW 02a00399 a0 = near white-car doubling): render DrivingForward G_erp on/near the L1 grid; use it (+LiDAR depth) to SELECT the correct L1 copy per masked column; alpha-composite the selected real pixels (NO diffusion). Lay 4 crops: E1.5 (doubled), referee-selected composite, no-referee single-copy, L1 ref.
- vision-gate: Does the car collapse to a SINGLE instance without smear/kink, far field untouched? If even the oracle copy-select fails, the referee mechanism is dead â†’ pivot to dataset/framework-only paper. If it works, the mechanism is real and worth a diffusion refiner.
- effort: ~1 A100-hour compute + standing up the DrivingForward env; 1â€“2 days incl. ray-table resample.

### [S1 â€” Minimal CertiPano on STUFF (refiner + masks + audit), iterate fast] Reproduce the user's liked effect from OUR backbone+mask on fresh frames, with far-field exactness and zero invented objects.
- steps: Build parallax-budget mask M + THINGS-vs-STUFF split; referee-select inside M; SDXL-inpaint (fast) with P_ctxt + object-negative, low CFG, strength 0.7, latent-clamp + hard re-paste + Poisson ring; run invention audit. Test on 20+ UNCURATED consecutive frames across all 5 logs at a SINGLE fixed operating point.
- vision-gate: On the uncurated strip: cars single / lines continuous / sky-ground clean; far field visibly unchanged + relative_warpâ‰ˆ0; SAM reports zero net-new salient objects. PASS = effect generalizes (not cherry-picked); near-doubling anchors may still ghost with plain inpaint â€” that is the expected E4 outcome and routes to S2, NOT to more CFG/mask tuning.
- effort: 1â€“1.5 weeks (SDXL avoids the ~10min FLUX-from-Drive load per run).

### [S2 â€” Productionize OOF outpaint + DiT360 refiner + boundary harmonization] Ship the near-certain visual win (full sphere) and the ERP-correct in-band refiner; lock the operating point.
- steps: DiT360 outpaint sky/ground on E1.5 across all logs with the invention guard; port the S1 refiner to DiT360 (circular wrap) where ERP-correctness matters; operating-point sweep (mask width Ã— CFG Ã— strength) â†’ (relative_warp, in-band drift, FID, invention-rate) frontier; provenance channel + manifest exporter.
- vision-gate: Full-sphere result holds across all 5 logs uncurated; a single fixed operating point gives no visible no-op seams and no hallucination; boundary has no sharpness/warp kink.
- effort: 1.5â€“2 weeks.

### [S3 â€” The novel head-to-head + the soft-vs-hard ablation (the method-paper gate)] Establish that fused-3DGS-as-referee beats prior art and that the mechanism is the fused referee, not hard depth.
- steps: Build the 4-way blind side-by-side (raw DiT360 / DiffPano / Difix3D+-over-DrivingForward / E1.5) vs CertiPano on the anchors + uncurated strips; run soft-referee vs hard-LiDAR-collapse ablation; measure guard recall via synthetic-object injection.
- vision-gate: CertiPano visibly beats all four baselines on the doubling anchors AND the soft-referee variant is not strictly worse than hard-LiDAR (so it is not rebranded E2). If neither holds â†’ demote to dataset/framework paper.
- effort: 2 weeks.

### [S4 â€” Downstream world-model utility probe (the decisive result)] Causal evidence that CertiPano data helps a world model without corrupting object stats.
- steps: Pre-register architecture/scale/task/metric/seeds; small proxy probe first (short-horizon/BEV on 5 logs); then â‰¥2 WM architectures Ã— â‰¥3 seeds on CertiPano vs L1 vs perspective; spurious-object audit on rollouts.
- vision-gate: CertiPano â‰¥ L1 and competitive-or-better than perspective on the rollout metric, with no object-hallucination inflation. Positive+robust â†’ main track; null â†’ honest D&B finding.
- effort: 3â€“4 weeks (highest variance; start the proxy early).

### [S5 â€” Scale, temporal stability (video), packaging] Dataset artifact + optional temporal-consistent video + paper figures.
- steps: Scale 5 logs â†’ full AV2 via content-hash DAG + ray-table caching; temporal wrapper (LiDAR+ego-flow warp of prior fill as init/clamp + per-track seed) gated on a temporal eval, ship stills first; curate the clean-360 dataset split + provenance; write up. (Waymo transfer = separate milestone needing new extrinsics + DrivingForward re-finetune; do not assume zero-shot.)
- vision-gate: No temporal swimming on a held-out clip; dataset reproducible from manifests; figures show uncurated strips not crops.
- effort: 2â€“3 weeks (video optional / can be cut).

## Eval protocol
FOUR AXES, vision-first, metrics second.

1) GEOMETRY / CERTIFIABILITY. (a) relative_warp(output, L1) on REAL â€” report as a sanity INVARIANT (â‰ˆ0 by construction; explicitly state it is a construction guarantee, not a test, to pre-empt the tautology critique). (b) THE REAL TEST: in-band metrics â€” straight-line / vanishing-point drift across the seam band (catches the warped-referee bent-line failure that Farneback-based relative_warp misses), boundary-continuity (gradient/structure match across the REALâ†”SYNTH edge), and a doubling-residual score. Build/validate this seam-band metric; note Farneback is blind to sub-pixel/textureless warp so it is NOT the in-band ruler.

2) PLAUSIBILITY. FID/KID vs real AV2 ERP-cropped street imagery + a blind human A/B study (single car? continuous building/lane? black sky/ground?) vs E1.5 and vs no-3DGS-referee fill â€” uncurated 20+ consecutive frames per log, not cherry-picked crops.

3) OBJECT SAFETY. Class-histogram delta (output vs source strips) + per-frame net-new salient-instance count + LiDAR-support cross-check; MEASURED guard recall via synthetic-object injection (paste known vehicles/people, report recall@conf at the dataset object-scale distribution) + human spot-check on a sampled set. Report an invention RATE with a detection-recall caveat; never claim zero.

4) DECISIVE â€” DOWNSTREAM WORLD-MODEL UTILITY (pre-registered BEFORE running; null is a publishable finding). Train an identical world-model probe on (i) CertiPano 360, (ii) L1 (doubled seams), (iii) raw perspective crops; compare held-out next-frame / BEV-occupancy / short-horizon rollout. â‰¥2 WM architectures, â‰¥3 seeds. Add a SPURIOUS-OBJECT audit on the trained model's rollouts: does CertiPano data inflate object hallucination vs L1/perspective? Claim to defend: CertiPano â‰¥ L1 and competitive-or-better than perspective for surround context, WITHOUT teaching wrong object statistics. Start with a SMALL proxy probe (short-horizon predictor on the 5 val logs) EARLY to confirm feasibility before building the full harness.

BASELINES (the four reviewers): (a) raw DiT360 inpaint/outpaint on our canvas (the "just use the tool" reviewer), (b) DiffPano (epipolar-attention prior art the referee most resembles), (c) Difix3D+ over DrivingForward's render (the 3DGS-refine reviewer), (d) plain E1.5 (the "do you even need diffusion" reviewer). CertiPano must visibly beat all four on the anchors or there is no method paper.

## Honest risks
1) METHOD NOVELTY IS THIN if the referee fails: strip the framing and S1/S3/S4 reduce to off-the-shelf components (DiT360 inpaint + PowerPaint + Difix3D+ pattern + SAM). The ONLY method-level novelty is fused-3DGS-as-referee; if S0/S3 don't show a clean win over DiffPano/Difix3D+, there is no main-track method paper and we must fall back to dataset/framework + a positive downstream result.
2) THE REFEREE LIVES IN THE UNDER-DETERMINED ZONE: the seam band is exactly where the view center is seen by no neighbor, so DrivingForward there is interpolating, and its render is blurry + globally warped (the team's own commit 925e525 retreated from the 3DGS render). A warped referee can bend L1's straight lines into a kink; mitigation is to use it only as a copy-SELECTOR (discrete choice), never as pixels/structure, but if even selection is unreliable the mechanism collapses to E2.
3) DEPTH-SENSITIVITY RELAPSE: LiDAR-collapse selection is depth-based correspondence, the thing that killed E2. The soft-vs-hard ablation is mandatory; if only hard-LiDAR works we have rebranded E2 and must say so.
4) OBJECT SAFETY IS THE HIGH-STAKES CLAIM AND CANNOT BE GUARANTEED: detectors miss small/occluded/partial objects (a half-car straddling a seam is the worst case). The THINGS-vs-STUFF construction makes it near-hard, but the headline must be "measured invention rate with stated guard recall," never "zero." A single invented vehicle across logs sinks the Bosch story.
5) DOWNSTREAM PROBE IS HIGH-COST, HIGH-VARIANCE, AND MAY RETURN NULL: it is the load-bearing result yet the most expensive; pre-register so a null is publishable, run a small proxy first, multi-seed/multi-arch to be credible.
6) GENERALIZATION, NOT PEAK QUALITY, IS THE HIDDEN WALL: the sweet-spot operating point is likely scene-dependent (night, low-texture, clutter); a fixed mask may no-op or hallucinate on some frames â€” the exact brittleness behind the 16 NEG versions. The uncurated-20-frame gate is designed to hit this early.
7) UNVERIFIED ASSETS/CITATIONS (I could not reach the local repo or Drive this session): the "E1.5 0.20px far-field, validated" number, results/dfwd_av2_finetune_v1, the DiT360 ckpt/outputs, and exact venues/arxiv IDs (Difix3D+, DrivingForward, DiT360, PowerPaint, DiffPano) must be independently re-confirmed before any claim or submission. Several "Confirmed on disk" assertions in the source designs were on Drive/Colab, not local.
8) S3a INTEGRATION GAP: current dibr_drivingforward_av2.py renders a fixed cubeâ†’ERP grid, NOT the L1 sphere ray-table; "same ray-table as L1" is a build item, not a fact.

## Open questions for user
- Track commitment: do you want to gate the project on the fused-3DGS-referee method paper (higher ceiling, higher risk â€” S0 decides), or lock in the dataset/benchmark + outpaint paper as the floor and treat the referee as a bonus ablation? The S0 oracle answers this in ~1 A100-hour â€” shall I treat its result as the binding decision?
- Downstream world model: which concrete world model / probe do we train for the utility test (an in-house Bosch model, or a public driving video/world model)? This is the decisive experiment and I need the target architecture + a held-out task/metric to pre-register it.
- Can you confirm or point me to: the E1.5 far-field number + deliverable path, results/dfwd_av2_finetune_v1 ckpt location, and DrivingForward's render quality on the 5 VAL logs (not just the finetune set)? The referee's reliability across logs is unproven and load-bearing.
- Object safety threshold for Bosch: what invention rate / guard recall is acceptable for training data, and is a visible honest CUT (E1.5 fallback) where a salient object straddles a seam preferable to any generative fill there? This sets how aggressive S1's generation can be.
- Is temporal-consistent VIDEO in scope for the first submission, or do we ship stills + the dataset first and treat video as follow-up? It is the biggest scope swing in S5.
- Compute/time budget: the staged plan is ~3â€“4 months of single-A100 work; if that is too long, which is the priority â€” the shippable outpaint+dataset paper (faster) or the referee method paper + downstream probe (slower, higher ceiling)?

**Target venues**: PRIMARY: NeurIPS Datasets & Benchmarks (or ICLR/ECCV dataset track) â€” the dataset + relative_warp/invention metrics + provenance contract + operating-point benchmark are first-class here and are the most defensible, lowest-variance contributions., UPGRADE TO MAIN TRACK (CVPR/ICCV/ECCV) â€” CONTINGENT on (a) the fused-3DGS-as-referee soft-vs-hard ablation yielding a clean win over DiffPano + Difix3D+-over-DrivingForward on the anchors, AND (b) a POSITIVE, robust (â‰¥2 WM architectures, â‰¥3 seeds) downstream utility result. Do not bet the submission on method novelty alone., FALLBACK / EARLY SIGNAL: a strong workshop (CVPR/ICCV workshop on autonomous driving or world models) to stake the formulation + certifiability framing while the downstream probe matures.

**Algorithm (final)**: CertiPano = a strict post-process over the existing L1/E1.5 backbone with three regions, two fillers, one referee, and one audit. Everything novel lives in how 3DGS and diffusion combine inside a geometry-derived seam band; L1 owns all geometry outside it.

STAGE 0 â€” BACKBONE (reuse verbatim; the geometry anchor).
- L1 hard_select: rigid sphere projection + per-pixel argmax â†’ clean ERP, straight lines, correct far field, plus a per-pixel select_id / overlap_count map. Artifacts = hard seams + near-field doubling at the ~7 overlaps only.
- E1.5 seam-confined low-freq multiband (multiband_lowfreq_blend, the critique confirmed this code is real and complete): kills the photometric seam; far field BYTE-IDENTICAL to L1 by construction (out[far]=base[far]); residual defect = the geometric doubling in the overlap band only. Base canvas C0 = E1.5.

STAGE 1 â€” PROVENANCE + PARALLAX-BUDGET MASK (the geometry IP).
- Provenance map: REAL (single-cam far field, never touched) / SEAM (overlap_countâ‰¥2 near-field band) / OOF (out-of-FoV sky/ground).
- Within SEAM, compute a per-ERP-column PARALLAX BUDGET d(px) â‰ˆ baselineÂ·f / depth from rig extrinsics + LiDAR ERP depth (conservative near-depth prior where LiDAR is sparse). Generative band M = columns where d > Ï„ (auto-narrows far â†’ near-no-op preserving real pixels; auto-widens only where near objects actually double). Trimap ring T = dilate(M, k) for harmonization. AREA-CAP rail: if M exceeds ~12% area, fall back to E1.5 (honest cut) rather than hallucinate.
- THINGS-vs-STUFF split (the world-model-safety fix from the critiques): run SAM/open-vocab detector on BOTH source strips. Any region containing a (partial) salient object â€” vehicle/person/cyclist/sign/pole â€” is REMOVED from the generative band M; there we composite the higher-confidence single L1 copy and blend only at its boundary. Generative fill is thus restricted to STUFF (road/lane/curb/facade/sky); generation never invents a THING. This converts "no invented objects" from a soft prompt into a near-hard construction.

STAGE 2 â€” THE NOVEL MECHANISM: FUSED-3DGS-AS-REFEREE (resolve the doubling).
- Run the AV2-finetuned DrivingForward (single ego optical center) to get a fused ERP render G_erp on the L1 ray-table (a build item â€” current code bakes a fixed cubeâ†’ERP grid; must be resampled onto L1's sphere or the mismatch documented). The fused render has physically committed both overlapping rays to ONE surface, so near-field doubling is gone â€” but it is blurry + globally low-freq warped, so it is NEVER used as final pixels.
- CRITICAL design correction from the critiques (do NOT double-condition one diffusion pass on disagreeing geometries): G_erp is used ONLY as a REFEREE / soft prior on WHICH of the two doubled L1 copies is the real instance and WHERE the single instance sits â€” not as an RGB/sharpness target. Concretely: G_erp (+ LiDAR depth ordering) selects, per masked column, the correct source copy; the chosen copy supplies sharp real pixels; the thin residual ghost sliver is what generation removes.
- This is the disambiguation single-frame depth-reprojection (proven-dead N1) and faithful flow/depth fusion (E2/E3) structurally lacked: the "which copy is real" answer comes from a multi-view-FUSED surface, not from one image's depth. ABLATION REQUIRED (the depth-sensitivity worry): soft-epipolar/fused-referee selection vs hard-LiDAR-collapse selection â€” if soft wins, that IS the defensible mechanism (the fused render picks the right correspondent where hard depth-reproject smears); if only hard-LiDAR works, it is rebranded E2 and must be reported as such.

STAGE 3 â€” SEAM-CONFINED DIFFUSION REFINER (sharpen + continue STUFF only).
- DiT360 (FLUX.1-dev + DiT360 LoRA, ERP circular wrap) OR fast SDXL-inpaint for iteration, in trimap-latent-clamp mode masked to M only. Init = the referee-selected sharp L1 copy (deghost-not-invent; denoise strength 0.6â€“0.8, NOT 1.0). PowerPaint P_ctxt structure-continuation token + positive "continue road/lane/curb/facade/sky" + P_obj-as-NEGATIVE "no new car/person/sign". LOW CFG (3â€“5; high CFG caused the E4 hallucinated trees).
- Region-exact sampler: per-step latent clamp (z_known=encode(C0) outside M) + HARD RE-PASTE after VAE decode (far field byte-identical by construction, provable). Boundary harmonized over ring T (Poisson/feather). This is the Difix3D+ "diffusion-fixes-degraded-3DGS" pattern, but (a) scoped to a thin seam band, (b) the 3DGS is a referee not a pixel source, (c) anchored by a rigid L1 co-base so the global warp can never enter the far field â€” the deltas Difix/ProSplat do not have.

STAGE 4 â€” OOF OUTPAINT (the near-certain shippable visual win).
- DiT360 outpaint extends the FoV band into black sky/ground under P_ctxt + object-negative. This is the result the user already likes; it is low-structure and object-free, acceptable under the PLAUSIBLE bar, and labeled SYNTH in provenance. Not novel â€” productionized, confined, guarded.

STAGE 5 â€” COMPOSE + AUDIT + EXPORT.
- Compose: start C0; overwrite ONLY M with Stage-3 output, ONLY OOF with Stage-4; re-paste REAL verbatim.
- OBJECT-INVENTION AUDIT (acceptance gate, every frame): detector class-histogram delta (output vs source strips) + LiDAR-support cross-check (a salient instance in a fill region with zero LiDAR footprint â†’ auto-reject) + measured guard recall via a SYNTHETIC-OBJECT INJECTION test (paste known cars/people, report recall@conf). Any net-new salient instance â†’ reject, fall back to E1.5 for that band, log it. Claim "audited, measured invention rate near zero on detectable instances," never "guaranteed."
- Export Bosch data spec: ERP master + per-pixel PROVENANCE channel (REAL / SEAM-SYNTH / OOF-SYNTH / GUARD-FALLBACK) + manifest.json (pose, stage git-shas, fraction_real/synth, guard_triggered, far-field warp, seam-band metrics) so the world model can down-weight synthesized pixels in its loss.

HOW L1 / 3DGS / DIFFUSION COMBINE (one sentence): L1 = clean geometry + far-field truth (byte-exact, certifiable); fused 3DGS = the referee that says which doubled copy is real inside the seam; diffusion = the seam-confined refiner that removes the residual ghost and continues STUFF, never THINGS, under a hard re-paste that keeps L1 untouched everywhere else.

**Paper story**: Autonomous-driving world models want surround context, but the raw surround rig is wide-baseline, low-overlap, and non-co-located, so a faithful 360Â° stitch is mathematically under-determined at the seams (we show this and exhaustively demonstrate why every faithful route fails). We argue the right target for TRAINING DATA is not source-faithfulness but a bounded relaxation: photoreal, structurally continuous, and free of invented salient objects, with the geometry that DOES have evidence kept exact and certifiable. CertiPano realizes this: a rigid projection backbone whose far field is provably byte-exact (a guarantee dense panorama diffusion cannot make), with synthesis confined to a thin, geometry-derived parallax-budget seam band. Inside that band we resolve the central artifact â€” near-field doubling â€” with our one new mechanism: a multi-view-fused 3DGS render acts as a referee that tells us which of the two doubled copies is real (impossible from a single frame's depth), after which a seam-confined diffusion refiner removes the residual ghost and continues road/building/sky, while THINGS are composited rather than generated so object statistics stay honest. We certify the result two ways the field currently cannot: a measured far-field/in-band geometric-fidelity ruler and a measured object-invention rate; and we close the loop with the decisive test â€” a world model trained on CertiPano data matches or beats one trained on L1/perspective on held-out rollout while not inflating object hallucination. The narrative honestly reframes "16 rejected versions": under the faithful bar they were correct rejections; the contribution is identifying the correct bar, the certifiability guarantee, the fused-referee mechanism, and the downstream proof.