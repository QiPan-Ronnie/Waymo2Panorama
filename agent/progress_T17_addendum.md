# T17 addendum - Panacea+ (arXiv 2408.07605) baseline recon (2026-05-21)

- **Headline (NEG)**: Panacea+ is **NOT** a viable downstream consumer of our L1 ERP / Pi3 .ply outputs. Modality mismatch is structural: Panacea+ consumes 8-channel BEV-layout rasters (3D bbox + HD-map + per-object depth + cam-pose) packed into nuScenes-only pkls, produces 6-cam 256-px multi-view video (not ERP); we produce 7-cam RGB -> 1024x2048 ERP + .ply. Dataset gap (no Gen-AV2 BEV pkls exist; ~1-2 weeks to build) + dep gap (pinned to Python 3.8 / torch 1.13.1+cu117 / mmcv-full 1.6.0 / mmdetection3d v1.0.0rc6, conflicts with Colab pt 2.10+cu128) + compute gap (8-GPU DeepSpeed default) all reinforce the modality gap. Recon job ran in 10.45 s on Colab A100 (commit `64f37b02`); git-clone path worked, classifier did not block. Joins T2 OmniStitch (-6.67 dB) + T18 Depth Pro (abs_rel 2.84x) as the third "naive prior-art transfer fails on AV2" data point.
- **Built**: `scripts/phase3/run_panacea_baseline.py` (412 lines, 3 modes - recon ran, install + inference documented-but-not-run as honest "cannot reproduce in time-box" disclosures). Probed repo confirms `inference.py`, `inference_nuscenes.yaml` (`in_channels=8`, `hint_channels=19`, FrameLength=8), `requirements/pt13.txt` (full pinned pre-2023 MMlab stack), `data_preparation.md` (requires `huggingface.co/datasets/orangewen/Gen-nuScenes`), `panacea.yml` (full conda env spec) all exist; `checkpoints/` and `data/nuscenes/` empty (must be user-supplied). HF ckpt `wenyuqing/Panacea-Plus` gated (HTTP 401 anonymous). Drive artifacts at `outputs/phase3/t17_panacea/` (summary.json + av2_anchor_grid.png + run_log.txt + 0-byte placeholder mp4).
- **Paper impact**: T7-prelim trigger row "T17 works -> add Section 6 demo" is closed negative; angle stays B-with-C-as-motivation, no restructuring needed. The negative is itself a paper data-point in Section 4 "vs prior art" or Section 5 "downstream consumers". Full analysis + modality-gap table + recommended ViPE-on-L1-ERP pivot in `notes/t17_panacea_report.md`. Recommended T17-FU: nothing; pivot downstream-consumer search to T9 (ViPE) when Colab has cycles.
