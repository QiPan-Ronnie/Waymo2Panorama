# T9 progress addendum

- **2026-05-21 ~04:55 UTC** — **ViPE (NVIDIA Video Pose Engine, panorama branch) runs end-to-end on our L1 ERP video.** Cloned `nv-tlabs/vipe@panorama` on Colab A100, installed via `pip install -r envs/requirements.txt -e .` (no conda needed; CUDA 12.8 base image works; one hiccup — protobuf 6.x in requirements.txt vs tensorflow 5.x, fixed with `pip install 'protobuf==5.28.3'`). Total install wall 465s (7m 46s).
- **Inference result** — ran `pipeline=panorama` on `baseline.mp4` (1024x2048, 20 Hz, 100 frames) in **96.7s wall** including model downloads (GroundingDINO + DeAOTL ~1.3 GB once). ViPE projected ERP into 4 horizontal + 1 bottom virtual pinhole views (panorama.yaml defaults), ran joint SLAM, wrote pose/intrinsics/masks to Drive (`outputs/phase3/t9_vipe/{pose,intrinsics,rgb,mask,vipe}/l1_erp.*`). Cosmetic font-not-found OSError in the final viz mp4 step — does NOT affect SLAM outputs.
- **Verdict** — **YES, paper Section 6 "downstream consumer demo" is real.** ViPE consumes our L1 stitched ERP and produces a usable SLAM trajectory + masks. Depth available with one flag (`pipeline.post.depth_align_model=dap`). This is the first concrete "stitched-RGB → published-downstream" data flow in the project (T17 ruled out Panacea+, Pantheon360 is parallel). Report: `notes/t9_vipe_on_av2_report.md`.
