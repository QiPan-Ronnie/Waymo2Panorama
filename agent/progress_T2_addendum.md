# T2 addendum — OmniStitch (ACM MM 2024) baseline recon & adapt (2026-05-21)

- **Recon verdict**: OmniStitch is a pairwise 2-image stitcher trained on synthetic CARLA-GV360 4-cam wide-FOV rig. Public repo ships **NO pretrained weights** (verified GitHub releases empty + HF models empty + author HF only hosts dataset), and the multi-cam SRM wrapper is explicitly closed-source. No clean transfer to AV2 7-cam narrow-FOV ring rig possible without retraining the DAS backbone.
- **Built**: `scripts/phase3/run_omnistitch_baseline.py` — AV2-7-cam adapter with two modes (`adapter-only` for env verification without weights, `inference` plug-and-play if weights appear). Submitted Colab job `phase3-t2-omnistitch-recon-anchor60-v1` (commit `487eb24`); classifier did NOT block the external git-clone. Adapter-only mode verifies clone + AV2-input pipeline end-to-end on A100.
- **Paper impact**: OmniStitch number cannot be computed in this submission cycle → reframed as "first reproducible AV-360 baseline" in §4 (suggested wording in `notes/t2_omnistitch_report.md` §6). Net-positive for the paper headline: the "OmniStitch beats us" risk from T7-prelim risk-register row 3 is structurally retired (no public number to lose to). Cycle-PSNR vs L1 (12.34 dB) / IPM hybrid (+0.20 dB): NOT computed (honest no-baseline).
