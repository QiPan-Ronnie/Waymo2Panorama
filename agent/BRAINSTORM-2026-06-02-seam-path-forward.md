# Seam path-forward — divergent + adversarial ideation (2026-06-02)

Source: workflow `wf_1fc2d59b-bb5` (6 ideation lenses × ~4 cards = 23, + 1 adversarial critic grounded in our specific NEG history). User asked to develop **two routes** — A = Google-Street-View-style plausible multi-center; B = DiT360 refined — toward the **ultimate goal: a near-perfect PLAUSIBLE seam** (no hallucinated objects). NOT building yet; this is the discussion menu.

## Adversarial ranking (best-first, grounded in our graveyard)
1. **[A] Difix-on-band** — band-confine the finetuned-DrivingForward 3DGS render (the ONE render verified doubling-free here) + a single-step reference refiner (Difix3D+), far field byte-exact L1 so global warp can't return. *Survives: uncertain.* Kill-test (30 min, zero diffusion): composite the EXISTING finetuned-3DGS render into the seam band only → relative_warp far-field≈0 + vision-check the band car is SINGLE (no doubling) though blurry. Settles "confinement kills global warp while keeping the fuse" before any GPU.
2. **[B] EPI-Mix family** — seam-confined diffusion with epipolar-masked + LiDAR-collapsed reference attention (depth only RE-WEIGHTS attention, never forward-warps → dodges the E2–E6 depth-error wall; epipolar mask from already-coded closed-form F makes off-line hallucination unreachable). The project's own "only untested principled wall-breaker." *Survives: uncertain.*
3. **[A] Per-seam adaptive convergence depth** — automatic "stitch-distance slider": one robust LiDAR-mode r* per seam (1-D line search, CPU, no per-pixel depth → can't smear). *Survives: likely as a bounded incremental win, NOT a full solver (bimodal near-car+mid-building depth in one wedge defeats a single r*).* ~1 CPU-hr decisive test.
4. **[B] PowerPaint structure-continuation + hard anti-object leash** (P_obj-negative + SAM/YOLO veto + LiDAR free-space). Targets the textureless mid-range where geometry has no evidence; complementary safe polish; abstains-to-E1.5 on hard frames. *Survives: likely; will NOT fix doubling.*
5. **[A] Multi-frame plane-sweep MVS in the seam wedge** — MEASURE depth from cross-view photo-consistency + a temporal ±2-frame real baseline (the documented "remaining real option" after E2 died; gives the mid-range building real parallax to triangulate). *Survives: uncertain* (textureless → flat cost volume = same starvation risk). Kill-test: cost-volume on BMW wedge, is the curve PEAKED on the mid-range surface?
6. **[A] LiDAR-seeded, epipolar-locked flow** — fix E3 by seeding RAFT with closed-form LiDAR disparity + collapsing to 1-D epipolar. *Survives: uncertain* — same starvation wall in a 1-D hat (the seed is densest where you least need it).
7. **[new] Ship the seam as an UNCERTAINTY CHANNEL** (provenance + parallax-budget + salient-object flag). Zero-hallucination, rides L1+E1.5. *Survives: likely, low ceiling* — value depends on Bosch consuming the channels (outside our control).
8. **[both] DEFINE the in-band seam metric + object-safety GATE** — the missing instrument (relative_warp is far-field-only & blind to in-band geometry + invention); would have killed the 16 DiT360 versions in minutes; certifies L1+E1.5 today; unblocks autonomous iteration. *Survives: likely as infrastructure, not a solver.* Kill-test: run the gate on the known-bad DiT360 outpaint (invented van/cars) vs clean L1.
9. **[A] Copy-SELECTION family** (parallax-budget routing, two-copy disambiguation, ConsensusGate). *Survives: probably-NOT — already NEG-adjacent:* log carries an explicit "selection route exhausted" verdict; depth-aware routing already moved <1% of pixels (NCC dropped to 0.82). Selection hides the photometric step but cannot remove the geometric offset.

## The ONE cheap kill-test that gates BOTH top routes (#1 & #2)
Render colorized LiDAR points into the BMW seam band and check (vision + NCC) whether LiDAR can disambiguate the correct (un-doubled) copy **on the MID-RANGE surface that actually doubles (the 10–30 m building/tree, NOT the near car)**. If LiDAR can't vote there, the refiner's geometry leash (#1) and the attention-collapse premise (#2) both weaken together → pivot to temporal MVS (#5) or frame-selection.

## What's MISSING (critic flag)
- **Frame/seam SELECTION as the deliverable.** This is a training-DATA corpus, not a live stitcher. Auto-detect which of the 7 seams in which frames are hard (high closed-form LiDAR parallax-budget AND a salient object on the cut), ship L1+E1.5 everywhere, and **down-select / time-shift** to frames where every seam is solvable → a clean PLAUSIBLE 360 corpus at near-zero risk without solving the worst seam. Choose the geometry, don't fight it.
- **Explicit SKY / upper-hemisphere plan.** LiDAR has ~0 upper returns (13.9% ERP support); several cards silently assume leash coverage above the horizon that doesn't exist.

## Synthesis (honest bottom line)
Two credible paths keep geometry RIGID and confine all risk to a thin band: **(1) Difix-on-band** (confinement structurally removes the global warp that got full-frame 3DGS rejected) and **(2) EPI-Mix** (depth re-weights attention, never warps → dodges the wall that killed E2–E6; epipolar + leave-one-strip-out leash gives a real target the held-out LoRA lacked). Both are genuinely unbuilt and share the one cheap LiDAR-copy-disambiguation kill-test. The selection family is effectively dead. Recommended cheap-first order: shared kill-test + per-seam-r* sweep + build the metric/gate (all cheap) → then commit GPU to whichever of #1/#2 survives the kill-test; keep PowerPaint + frame-selection as the guaranteed-clean-corpus floor.

(Note: the staged DiT360 center-outpaint **v2** re-run is a SEPARATE low-stakes "看效果" for Koi — a generator demo, NOT one of these seam-solving routes.)
